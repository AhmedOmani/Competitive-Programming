#include <bits/stdc++.h>
#define all(v) v.begin() , v.end()
#define Pi 3.14159
using namespace std;

using ll = long long;

ll gcd(ll a, ll b) { return ((b == 0) ? a : gcd(b, a % b)); }
ll lcm(ll a, ll b) { return (b / gcd(a, b)) * a; }


void Omani () {
ios::sync_with_stdio(0); cin.tie(0), cout.tie(0);
#ifndef ONLINE_JUDGE
freopen("input.txt", "r", stdin);
#endif
#ifdef _LOCAL_DEFINE
  cerr << (double)clock() * 1.0 / CLOCKS_PER_SEC << endl;
#endif
}

string letters = "0123456789ABCDEF" ;

int ToInt(char & c){
    return letters.find(c) ;
}


int FromAnyBaseToDecimal(string &num , int base) { /// Call the number & the base you want to convert

    int res = 0 ;

    for (int i = 0 ; i < num.size() ; i++)
        res *= base , res += ToInt(num[i]) ;

    return res ;
}

string FromDecimalToAnyBase(int num , int base) { /// Call the number & the base you want to convert

    if (num == 0)
        return "0" ;

    string res = "" ;

    while (num)
        res = letters[num % base] + res , num /= base ;

    return res ;

}

void Solve() {

}

int main() {
Omani() ;
int T = 1 ;                                   ///cin >> T ;
while(T--) Solve() ;
}
