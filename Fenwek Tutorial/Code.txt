#include <bits/stdc++.h>
#define all(v) v.begin() , v.end()
#define Pi 3.14159
#define sz(x) int(x.size())
#define MOD int(1e9 + 7) ;
using namespace std;

using ll = long long;

ll gcd(ll a, ll b) { return ((b == 0) ? a : gcd(b, a % b)); }
ll lcm(ll a, ll b) { return (b / gcd(a, b)) * a; }


void Omani () {
ios::sync_with_stdio(0); cin.tie(0), cout.tie(0);
#ifndef ONLINE_JUDGE
freopen("input.txt", "r", stdin);
#endif
#ifdef _LOCAL_DEFINE
  cerr << (double)clock() * 1.0 / CLOCKS_PER_SEC << endl;
#endif
}

const int N = 1e6 + 10 ;
int FenwekTree[N] ;

void Update(int idx , int val) {

    while (idx > 0 && idx < N)
        FenwekTree[idx] += val , idx +=( (idx) & (-idx) ) ;

}

ll sum(int idx) {

    ll S = 0 ;

    while (idx > 0)
        S += FenwekTree[idx] , idx = idx - ( idx & (-idx) ) ;

    return S ;

}

ll Prefix(int L , int R) {

    return sum(R) - sum(L - 1);
}

void Solve() {

 

}


int main() {
Omani() ;
int T = 1 ;                                  ///cin >> T ;
while(T--) Solve() ;
}
