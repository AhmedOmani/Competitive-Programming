/// Calculate the value of n choose r using the binomial coefficient formula

    int n = (n - 4) + 2 ;
    int r = 2 ;

    double sum = 1;

    for(int i = 1; i <= r ; i++)
        sum = sum * (n - r + i) / i;

    cout << (long long) sum <<endl;

/// Calculate Calculate the value of n choose r using recursion

	ll nCr(int n , int r ) {
		if (n<r) return 0 ;
		if(n==r || r==0) return 1 ;
 
		return nCr(n-1,r-1) + nCr(n-1,r) ;
	}